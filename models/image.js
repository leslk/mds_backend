class Image {
    constructor(imageUrl) {
        this.imageUrl = imageUrl;
    }

    generate(prompt) {
        // Call Stable to generate image with prompt
        // receive generated image and store in server
        // Store image in imageUrl
    }
}