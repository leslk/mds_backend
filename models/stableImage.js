class StableImage {
    constructor(prompt) {
        this.prompt = prompt;
    }

    static fromPrompt(prompt) {
        
    }
    getImage() {
        // call the stableImg API to get the image
    }
}