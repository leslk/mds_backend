class OpenAi{
    // constructor(text) {
    //     //this.text = text;
    // }

    async funtgenerateUniverseDescription(universeName) {
        return // openaicall(`Generate a description for a universe named ${universeName}`)
    }

    generatePrompt(universe) {
        // call the openAi API to get the description
        // return description
    }
}